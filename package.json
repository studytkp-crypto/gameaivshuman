{
  "name": "human-vs-ai-platform",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm --prefix client run build",
    "prisma:deploy": "npx prisma db push --schema=./server/prisma/schema.prisma && node server/prisma/seed.js",
    "start": "node server/src/index.js"
  },
  "dependencies": {}
}